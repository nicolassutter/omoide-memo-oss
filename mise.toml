[tools]
mprocs = "latest"

[tasks.dev]
description = "Generate SDK then start api + dashboard concurrently"
run = [
  "cd packages/telemetry-api && mise run generate:openapi",
  "cd packages/telemetry-dashboard && pnpm run generate:openapi",
  "mprocs",
]
