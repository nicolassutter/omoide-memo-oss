# Shared API key between telemetry-api and telemetry-dashboard.
# Must match in both services. Override in production.
TELEMETRY_API_KEY=dev-secret-key

# Postgres credentials used by the postgres service.
# DATABASE_URL is composed in compose.yaml from these values.
POSTGRES_USER=telemetry
POSTGRES_PASSWORD=telemetry
POSTGRES_DB=telemetry
