services:
  tailscale:
    image: tailscale/tailscale:latest
    restart: unless-stopped
    hostname: omoide-oss-caddy
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY}
      - TS_STATE_DIR=/var/lib/tailscale
    volumes:
      - tailscale_state:/var/lib/tailscale
    cap_add:
      - net_admin
      - net_raw

  # tailscale handles its traffic
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    network_mode: "service:tailscale"
    depends_on:
      - tailscale
    environment:
      - API_PROXY_URL=${API_PROXY_URL:-http://telemetry-api:9999}
      - TELEMETRY_ADMIN_API_KEY=${TELEMETRY_ADMIN_API_KEY}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U "$$POSTGRES_USER" -d "$$POSTGRES_DB"
      interval: 5s
      timeout: 5s
      retries: 10

  # served from outside hence the exposed port
  telemetry-api:
    image: ${IMAGE_PREFIX:-}telemetry-api:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: api-runtime
      args:
        GO_VERSION: ${GO_VERSION:-1.27.1}
    restart: unless-stopped
    ports:
      - "9999:9999"
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      - TELEMETRY_PUBLIC_API_KEY=${TELEMETRY_PUBLIC_API_KEY}
      - TELEMETRY_ADMIN_API_KEY=${TELEMETRY_ADMIN_API_KEY}
      - PORT=9999
      - ALLOWED_ORIGIN=${ALLOWED_ORIGIN:-*}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - wget --no-verbose --tries=1 --spider http://localhost:9999/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # served by caddy, check Caddyfile
  telemetry-dashboard:
    image: ${IMAGE_PREFIX:-}telemetry-dashboard:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: dashboard-runtime
      args:
        GO_VERSION: ${GO_VERSION:-1.27.1}
        NODE_VERSION: ${NODE_VERSION:-26}
        PNPM_VERSION: ${PNPM_VERSION:-12}
    restart: unless-stopped
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - NODE_ENV=production
    depends_on:
      telemetry-api:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - curl -fsS http://localhost:3000/health || exit 1
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  caddy_data:
  postgres_data:
  tailscale_state:
